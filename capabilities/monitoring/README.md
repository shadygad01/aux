# Monitoring Capability

Owns aggregation of capability-provided health contracts into `SystemHealth`. Input: injected `HealthProvider` interfaces. Outputs include per-capability and aggregate states. It records check metrics/logs and contains no business decision logic.
