# Reasoning Capability

Owns creation of a complete macro-to-recommendation reasoning path through `ReasoningPort`. Input: `ReasoningInput`. Output: `ReasoningDecision`. Metric: `reasoning_paths`. Logs reasoning IDs. Health confirms configured reasoning implementation. It does not publish decisions.

Reasoning cannot begin without a current market regime and a regime interpretation for every input
evidence item.
