# Applications

Deployable presentation surfaces live here. Applications may compose packages and adapters but must not own business rules. Two composition roots exist today: `decision_cli` (attention-worthiness, `DecisionEngine`) and `trading_cli` (setup-search permission, `TradingOpportunityEngine`). Future APIs or dashboards require separate folders and composition roots.
