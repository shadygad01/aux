# Published dashboard

`index.html` and `app.js` render `artifacts/market_data.json`. No other artifact
is a supported product input. See `architecture.md` for the retained facts and
fail-closed exclusions.
